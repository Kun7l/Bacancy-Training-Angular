import { AdharNumberMaskPipe } from './adhar-number-mask-pipe';

describe('AdharNumberMaskPipe', () => {
  it('create an instance', () => {
    const pipe = new AdharNumberMaskPipe();
    expect(pipe).toBeTruthy();
  });
});
