import { AppRepeat } from './app-repeat';

describe('AppRepeat', () => {
  it('should create an instance', () => {
    const directive = new AppRepeat();
    expect(directive).toBeTruthy();
  });
});
