import { AppHighlightDiscount } from './app-highlight-discount';

describe('AppHighlightDiscount', () => {
  it('should create an instance', () => {
    const directive = new AppHighlightDiscount();
    expect(directive).toBeTruthy();
  });
});
