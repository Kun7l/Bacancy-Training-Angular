import { AppStatusBorder } from './app-status-border';

describe('AppStatusBorder', () => {
  it('should create an instance', () => {
    const directive = new AppStatusBorder();
    expect(directive).toBeTruthy();
  });
});
