import { AppLoading } from './app-loading';

describe('AppLoading', () => {
  it('should create an instance', () => {
    const directive = new AppLoading();
    expect(directive).toBeTruthy();
  });
});
