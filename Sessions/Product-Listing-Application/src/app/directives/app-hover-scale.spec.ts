import { AppHoverScale } from './app-hover-scale';

describe('AppHoverScale', () => {
  it('should create an instance', () => {
    const directive = new AppHoverScale();
    expect(directive).toBeTruthy();
  });
});
